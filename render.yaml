services:
  - type: web
    name: blueprint
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: FMP_API_KEY
        sync: false
